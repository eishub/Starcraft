use MapAgent as knowledge.
use "../Shared" as knowledge.

module MapAgentInit {
	% Initialize the resources belief (on-change percept)
	if percept(resources(M,G,CS,TS)) 
		then insert(resources(M,G,CS,TS)).
	if percept(researched(L)) 
		then insert(researched(L)).
}