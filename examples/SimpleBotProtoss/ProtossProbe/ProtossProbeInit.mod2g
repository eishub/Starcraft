use ProtossProbe as knowledge.

module ProtossProbeInit {
	% Initialize the conditions belief (on-change percept)
	if percept(status(_,_,_,Conditions,_,_,_,_)) 
		then insert(conditions(Conditions)).
}